# BankApp Notification Service Environment Configuration
# Copy this file to notification-service.env and fill in the values
# Usage: docker compose -f notification-service.yml --env-file notification-service.env up

# -- Docker Hub Config --
# Required for pulling the notification service image from Docker Hub
DOCKER_REGISTRY_USER=
DOCKER_REGISTRY_PASSWORD=
DOCKER_REGISTRY_URL=

# -- Global Settings --
# Sets the project name for Docker Compose, which prefixes container and network names
COMPOSE_PROJECT_NAME=my_infra

# -- RabbitMQ Connection Configuration --
# The notification service connects to RabbitMQ to consume messages
# These should match your RabbitMQ deployment configuration
RABBIT_MQ_HOST=rabbitmq
RABBITMQ_DEFAULT_USER=guest
RABBITMQ_DEFAULT_PASS=guest

# -- Resend API Configuration --
# API key for Resend email service (required)
RESEND_API_KEY=

# -- Email Configuration --
# The email address to use as the sender for notifications
SENDER_EMAIL_ADDRESS=
