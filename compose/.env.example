# Modular Environment Configuration
# This directory now contains separate .env.example files for each compose stack.
# Choose the approach that best fits your deployment needs:

# OPTION 1: Use separate environment files for each stack (RECOMMENDED)
# Copy the specific .env.example files for the stacks you want to deploy:
# - .env.auth-stack.example     -> .env.auth-stack     (for auth-stack.yml)
# - .env.infra-proxy.example    -> .env.infra-proxy    (for infra-proxy.yml)
# - .env.infra-messaging.example -> .env.infra-messaging (for infra-messaging.yml)
# - .env.notification-service.example -> .env.notification-service (for notification-service.yml)
# 
# Then use: docker compose -f <stack>.yml --env-file .env.<stack> up

# OPTION 2: Use a single .env file for all stacks (LEGACY)
# Copy this file to .env and fill in all the values below.
# This approach works but requires managing all configuration in one place.

# Docker Hub Config
DOCKER_REGISTRY_USER=
DOCKER_REGISTRY_PASSWORD=
DOCKER_REGISTRY_URL=

# -- Global Settings --
# Sets the project name for Docker Compose, which prefixes container and network names.
COMPOSE_PROJECT_NAME=my_infra

# -- PostgreSQL Configuration --
# The user for the PostgreSQL database.
POSTGRES_USER=admin
# The password for the PostgreSQL user. Use a strong, unique password.
POSTGRES_PASSWORD=your_strong_password
# The name of the database to be created on startup.
POSTGRES_DB=auth-service
# The external port to map to the PostgreSQL container's port 5432.
POSTGRES_PORT=5432

# -- Redis Configuration --
# The password to protect the Redis instance.
REDIS_PASSWORD=your_strong_redis_password
# The external port to map to the Redis container's port 6379.
REDIS_PORT=6379

# -- RabbitMQ Configuration --
# The default user for RabbitMQ.
RABBITMQ_DEFAULT_USER=guest
# The password for the default RabbitMQ user. Change this for production environments.
RABBITMQ_DEFAULT_PASS=guest
# The hostname for the RabbitMQ node. Important for data persistence.
RABBITMQ_NODE_HOSTNAME=my-rabbit
# The external port for the AMQP protocol (for application clients).
RABBITMQ_AMQP_PORT=5672
# The external port for the RabbitMQ Management UI.
RABBITMQ_MANAGEMENT_PORT=15672

# -- INFRA-proxy
# -- Nginx Configuration --
# The external port to map for HTTP traffic.
NGINX_HTTP_PORT=80
# The external port to map for HTTPS traffic.
NGINX_HTTPS_PORT=443
# Cloudflared
CLOUDFLARED_TUNNEL_TOKEN=

# -- BankApp Auth Service --
# -- put your auth server hostname here
PASSKEY_RPID=localhost
# -- pub your FE hostname here
PASSKEY_ORIGIN=http://localhost
# Spring Config
SPRING_PROFILES_ACTIVE=prod
# Server config
PORT=8080
# Dependencies configuration (configured for docker-compose)
RABBIT_MQ_HOST=rabbitmq
REDIS_HOST=redis
POSTGRES_HOST=postgres

# -- BankApp Notification Service --
RESEND_API_KEY=
SENDER_EMAIL_ADDRESS=
